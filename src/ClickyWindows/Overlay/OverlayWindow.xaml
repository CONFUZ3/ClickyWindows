<Window x:Class="ClickyWindows.Overlay.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStyle="None"
        Topmost="True"
        ShowInTaskbar="False"
        IsHitTestVisible="False"
        ResizeMode="NoResize">
  <Canvas x:Name="RootCanvas" IsHitTestVisible="False">
    <!-- Triangle cursor -->
    <Polygon x:Name="CursorTriangle"
             IsHitTestVisible="False"
             Visibility="Visible">
      <Polygon.Effect>
        <DropShadowEffect Color="#3380FF" BlurRadius="12" ShadowDepth="0" Opacity="0.7"/>
      </Polygon.Effect>
    </Polygon>

    <!-- Waveform bars (audio recording) -->
    <StackPanel x:Name="WaveformPanel"
                Orientation="Horizontal"
                Visibility="Collapsed"
                IsHitTestVisible="False">
      <Rectangle x:Name="Bar1" Width="4" Fill="#3380FF" RadiusX="2" RadiusY="2" Margin="1,0"/>
      <Rectangle x:Name="Bar2" Width="4" Fill="#3380FF" RadiusX="2" RadiusY="2" Margin="1,0"/>
      <Rectangle x:Name="Bar3" Width="4" Fill="#3380FF" RadiusX="2" RadiusY="2" Margin="1,0"/>
      <Rectangle x:Name="Bar4" Width="4" Fill="#3380FF" RadiusX="2" RadiusY="2" Margin="1,0"/>
      <Rectangle x:Name="Bar5" Width="4" Fill="#3380FF" RadiusX="2" RadiusY="2" Margin="1,0"/>
    </StackPanel>

    <!-- Processing spinner -->
    <Canvas x:Name="SpinnerCanvas" Visibility="Collapsed" IsHitTestVisible="False"
            Width="32" Height="32">
      <Ellipse x:Name="SpinnerArc" Width="28" Height="28"
               Canvas.Left="2" Canvas.Top="2"
               StrokeThickness="3" Fill="Transparent">
        <Ellipse.Stroke>
          <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#3380FF" Offset="0"/>
            <GradientStop Color="Transparent" Offset="1"/>
          </LinearGradientBrush>
        </Ellipse.Stroke>
        <Ellipse.RenderTransform>
          <RotateTransform x:Name="SpinnerRotate" CenterX="14" CenterY="14"/>
        </Ellipse.RenderTransform>
      </Ellipse>
    </Canvas>

    <!-- Speech bubble -->
    <Border x:Name="SpeechBubble"
            Visibility="Collapsed"
            Background="#CC1A1A2E"
            CornerRadius="8"
            Padding="8,4"
            IsHitTestVisible="False">
      <Border.Effect>
        <DropShadowEffect Color="#3380FF" BlurRadius="8" ShadowDepth="0" Opacity="0.5"/>
      </Border.Effect>
      <TextBlock x:Name="SpeechText"
                 Foreground="White"
                 FontFamily="Segoe UI"
                 FontSize="13"
                 FontWeight="SemiBold"/>
    </Border>
  </Canvas>
</Window>
